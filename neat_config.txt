# NEAT Configuration for Racing Controller Parameter Optimization
# Based on best practices from https://neat-python.readthedocs.io/en/latest/cookbook.html

[NEAT]
# Population size - larger for more diversity
pop_size = 100
# Fitness criterion: max = maximize fitness (higher is better)
fitness_criterion = max
# Don't terminate on fitness threshold - we optimize continuously
fitness_threshold = 99999
# Reset on extinction to keep evolution going
reset_on_extinction = True
# No fitness termination - run for specified generations
no_fitness_termination = False

[DefaultGenome]
# Network topology
# 1 dummy input (NEAT needs at least 1 input to work correctly)
num_inputs = 1
# 8 outputs - one for each tunable parameter
num_outputs = 8
# No hidden nodes needed for static parameter output
num_hidden = 0

# Node activation options
# Use tanh for outputs in range [-1, 1] which we scale to parameter ranges
activation_default = tanh
activation_mutate_rate = 0.0
activation_options = tanh

# Node aggregation options
aggregation_default = sum
aggregation_mutate_rate = 0.0
aggregation_options = sum

# Node bias options - biases ARE the evolved parameters, so use wide init and strong mutations
bias_init_mean = 0.0
bias_init_stdev = 3.0
bias_init_type = gaussian
bias_max_value = 30.0
bias_min_value = -30.0
bias_mutate_power = 1.5
bias_mutate_rate = 0.9
bias_replace_rate = 0.2

# Genome compatibility options
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient = 0.5

# Connection add/remove rates - MUST be 0 with num_inputs=0
conn_add_prob = 0.0
conn_delete_prob = 0.0

# Connection enable options
enabled_default = True
enabled_mutate_rate = 0.01
enabled_rate_to_true_add = 0.0
enabled_rate_to_false_add = 0.0

# Feed-forward network (no recurrence needed for static params)
feed_forward = True

# Connection weight options - full connection from dummy input to all outputs
initial_connection = full_direct
weight_init_mean = 0.0
weight_init_stdev = 1.0
weight_init_type = gaussian
weight_max_value = 30
weight_min_value = -30
weight_mutate_power = 0.5
weight_mutate_rate = 0.8
weight_replace_rate = 0.1

# Node add/remove rates - MUST be 0 with num_inputs=0 (no connections to split)
node_add_prob = 0.0
node_delete_prob = 0.0

# Node response options
response_init_mean = 1.0
response_init_stdev = 0.0
response_init_type = gaussian
response_max_value = 30.0
response_min_value = -30.0
response_mutate_power = 0.0
response_mutate_rate = 0.0
response_replace_rate = 0.0

# Structural mutation inheritance
single_structural_mutation = False
structural_mutation_surer = default

[DefaultSpeciesSet]
# Compatibility threshold - lower = more species = more diversity
compatibility_threshold = 3.0

[DefaultStagnation]
# Species fitness function
species_fitness_func = max
# Max generations without improvement before species is removed
max_stagnation = 20
# Always keep at least this many species
species_elitism = 2

[DefaultReproduction]
# Number of top genomes to preserve unchanged each generation
elitism = 3
# Fraction of species allowed to reproduce
survival_threshold = 0.2
# Minimum number of genomes in a species
min_species_size = 2

